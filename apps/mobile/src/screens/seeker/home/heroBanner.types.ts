/** Shared types for the hero banner / carousel components. */

/** Translation function signature — matches what `useTranslate()` returns. */
export type TFn = (key: string, opts?: Record<string, unknown>) => string;
